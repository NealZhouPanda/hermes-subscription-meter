# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node
node_modules/

# Editor / temp
*.swp
*~
.tmp/

# Screenshots with real personal data must never be committed
docs/images/*.png
!docs/images/.gitkeep

# Provider research: the directory is the source of truth, the zip is a local
# archive twin of it — committing both would let the two copies drift
docs/provider-research/*.zip
