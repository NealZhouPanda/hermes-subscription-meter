name: subscription-meter
kind: standalone
version: 0.2.0
description: 订阅额度看板：供应方可自助显隐，套餐显示 84 格，账户显示余额。
provides_tools: []
config_schema:
  visibility:
    type: dict
    description: 供应方显示开关（供应方 id → 是否在额度看板显示）
